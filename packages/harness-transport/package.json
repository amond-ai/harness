{
  "name": "@amond-ai/harness-transport",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "The bridge socket a turn driver speaks over \u2014 the `ws`-shaped socket shim, the upgrade handshake, and a dialer built on the standard WebSocket constructor",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "bun --bun vitest run",
    "test:coverage": "bun --bun vitest run --coverage"
  },
  "devDependencies": {
    "@pleaseai/eslint-config": "catalog:",
    "@vitest/coverage-istanbul": "catalog:",
    "bun-types": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}

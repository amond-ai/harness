{
  "name": "@pleaseai/sandbox-contract",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "The sandbox surface the orchestrator runs against — owned here, not borrowed, so a backend other than Cloudflare can satisfy it",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "bun --bun vitest run"
  },
  "devDependencies": {
    "@pleaseai/eslint-config": "catalog:",
    "bun-types": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
